import type { SessionStatus } from './types';

export const sessionStatus: SessionStatus = {
  // Here more status...
  ONLINE: 'online', // Well
  OFFLINE: 'offline', // Well again
  // If adding is needed
};
